# Overlay for Cloudflare Tunnel deployments.
# Usage:
#   docker compose --profile default --profile tunnel \
#     -f docker-compose.yml -f docker-compose.tunnel.yml up -d

services:
  jukebox:
    ports: !reset []
    environment:
      CLOUDFLARE_TUNNEL: "1"
