# Copy to .env.production — used by all Docker profiles (local, cloudflare, tailscale).
#
# Generate: openssl rand -hex 32   # ENCRYPTION_KEY

SPOTIFY_CLIENT_ID=30dda24a40174e69b03891fb3cd0a007
SPOTIFY_CLIENT_SECRET=85f172a7407d490e8ac1e12b1ab444ed
SPOTIFY_REDIRECT_URI=https://jukebox.currannet.net/api/v1/host/spotify/callback
BASE_URL=https://jukebox.currannet.net
ENCRYPTION_KEY=5e3eabb3aec1e9c8f360aaf7d0a4a628c872b00eb903e058567df37dd32cde54
HOST_SETUP_TOKEN=set-this-to-something-secret-or-remove-to-disable-host-session-token

# ── Optional ─────────────────────────────────────────────────────────────────
# ALLOW_INSECURE_HTTP=1      # http:// LAN parties (match Spotify redirect URI)

# ── Sync (optional) ─────────────────────────────────────────────────────────
# SYNC_FAST_POLL=1
SYNC_END_WINDOW_MS=7000
SYNC_FALLBACK_INTERVAL_MS=30000
SYNC_IDLE_INTERVAL_MS=60000

# ── Spotify API budget (optional) ───────────────────────────────────────────
# SPOTIFY_API_BUDGET_COUNT=90
# SPOTIFY_API_BUDGET_WINDOW_MS=30000
# SPOTIFY_DAILY_WARN_CALLS=8000