# Copy to .env.cloudflared (gitignored)
# Used with: docker compose --profile cloudflare up --build -d
#
# Token: Cloudflare Zero Trust → Networks → Tunnels → your tunnel → Install connector

TUNNEL_TOKEN=
