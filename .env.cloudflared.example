# Cloudflare Tunnel — copy to .env.cloudflared (gitignored)
# Optional. Only used with docker-compose.cloudflare.yml:
#
#   bun run docker:up:cloudflare
#
# Get the token: Cloudflare Zero Trust → Networks → Tunnels → your tunnel

TUNNEL_TOKEN=
